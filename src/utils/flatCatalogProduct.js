export async function flatCatalogProduct(catalogProduct) {
    const {
        id,
        name,
        description,
        price,
        image,
        category,
        createdAt,
        updatedAt,
    } = catalogProduct;
    flattenFields = {};
}